# Path to the root of the downloaded dataset folder (relative to this file's location/training script execution dir)
path: ./ATM-Theft-Detection-4

# Directories for train, validation, and test images (relative to the path above)
train: train/images
val: valid/images
test: test/images

# Number of classes
nc: 2

# Merged class names
names:
  0: Face_Covered
  1: Face_Uncovered